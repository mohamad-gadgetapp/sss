import React from "react";
import "./style.css";

const Button = () => {
  return (
    <div>
      <button type="submit" className="submit-Btn" disabled>
        Submit
      </button>
    </div>
  );
};

export default Button;
